{
  "name": "react-hooks-mock-code-challenge-plantshop",
  "version": "0.1.0",
  "private": true,

  "scripts": {
    "dev": "vite",
    "server": "json-server db.json -p 6001",
    "test": "vitest",
    "build": "vite build"
  },

  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  }
}